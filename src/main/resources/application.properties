server.servlet.context-path=/customer-management-api
server.port=8070

# LOGGING Config
logging.file.name=C:\\Users\\vlads\\OneDrive\\Documents\\logs\\api-customermanagement.log
logging.level.root= INFO
logging.level.org.springframework.boot= ERROR


# === [START] DATABASE Config ===
## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres

### These are set as runtime JVP Variables to hide sensitive information. JVM Options: -DAPP_USERID=postgres -DAPP_PASSWORD=
spring.datasource.username=${APP_USERID}
spring.datasource.password=${APP_PASSWORD}

#drop n create table again, good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === [END] DATABASE Config ===


